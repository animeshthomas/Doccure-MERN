{
  "name": "doccure-mern",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "cd backend && npm install",
    "start": "cd backend && node index.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
